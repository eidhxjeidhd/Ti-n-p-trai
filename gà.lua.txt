-- TIẾN HUB ULTRA PREMIUM - Short Version
-- All Premium Features - Compact Code
-- Created by Tiến

if not game:IsLoaded() then wait(game.Loaded) end

local Player = game.Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()

-- Ultra Config
getgenv().Config = {
    SuperPunch = {Enabled = false, Range = 2000, Damage = 10000, Cooldown = 0.2},
    AutoFarm = {Enabled = false, Range = 1500, StandStill = true},
    Player = {WalkSpeed = 16, JumpPower = 50, NoClip = false},
    Abilities = {AutoBuso = false, AutoKen = false}
}

-- Ultra GUI
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("🌟 TIẾN HUB ULTRA PREMIUM", "Sentinel")

-- Toggle Image
local ToggleGui = Instance.new("ScreenGui")
local ToggleBtn = Instance.new("ImageButton")
ToggleGui.Parent = game.CoreGui
ToggleBtn.Parent = ToggleGui
ToggleBtn.Size = UDim2.new(0, 60, 0, 60)
ToggleBtn.Position = UDim2.new(0, 20, 0.5, -30)
ToggleBtn.Image = "rbxassetid://105896068771440"
ToggleBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
ToggleBtn.MouseButton1Click:Connect(function() Library:ToggleUI() end)

-- 💥 COMBAT TAB
local CombatTab = Window:NewTab("💥 Combat")
local CombatSection = CombatTab:NewSection("Ultra Combat System")

CombatSection:NewToggle("🚀 Ultra Super Punch", "Đấm siêu xa 2000 studs", function(state)
    getgenv().Config.SuperPunch.Enabled = state
    if state then spawn(function()
        while getgenv().Config.SuperPunch.Enabled do wait(0.2)
            pcall(function()
                for _, enemy in pairs(workspace.Enemies:GetChildren()) do
                    if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
                        local dist = (Character.HumanoidRootPart.Position - enemy.HumanoidRootPart.Position).Magnitude
                        if dist <= getgenv().Config.SuperPunch.Range then
                            enemy.Humanoid.Health -= getgenv().Config.SuperPunch.Damage
                            enemy.HumanoidRootPart.Velocity = (enemy.HumanoidRootPart.Position - Character.HumanoidRootPart.Position).Unit * 200
                        end
                    end
                end
                for i = 1, 3 do game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Click") end
            end)
        end
    end) end
end)

CombatSection:NewToggle("👊 One Punch Mode", "1 đấm chết tất cả", function(state)
    getgenv().OnePunchMode = state
    if state then spawn(function()
        while getgenv().OnePunchMode do wait(0.5)
            pcall(function()
                for _, enemy in pairs(workspace.Enemies:GetChildren()) do
                    if enemy:FindFirstChild("Humanoid") then enemy.Humanoid.Health = 0 end
                end
            end)
        end
    end) end
end)

-- 🌿 AUTO FARM TAB
local FarmTab = Window:NewTab("🌿 Auto Farm")
local FarmSection = FarmTab:NewSection("Smart Auto Farm")

FarmSection:NewToggle("🤖 Auto Farm Stand Still", "Đứng yên farm level", function(state)
    getgenv().Config.AutoFarm.Enabled = state
    if state then spawn(function()
        while getgenv().Config.AutoFarm.Enabled do wait(0.1)
            pcall(function()
                local enemyCount = 0
                for _, enemy in pairs(workspace.Enemies:GetChildren()) do
                    if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
                        local dist = (Character.HumanoidRootPart.Position - enemy.HumanoidRootPart.Position).Magnitude
                        if dist <= getgenv().Config.AutoFarm.Range then
                            enemyCount += 1
                            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Click")
                        end
                    end
                end
                if getgenv().Config.AutoFarm.StandStill then
                    Character.HumanoidRootPart.Velocity = Vector3.new(0, 0, 0)
                end
            end)
        end
    end) end
end)

FarmSection:NewToggle("👑 Auto Farm Boss", "Tự động farm boss", function(state)
    getgenv().AutoFarmBoss = state
end)

-- 🎮 PLAYER TAB
local PlayerTab = Window:NewTab("🎮 Player")
local PlayerSection = PlayerTab:NewSection("Player Enhancements")

PlayerSection:NewSlider("💨 WalkSpeed", "Tốc độ chạy", 500, 16, function(value)
    Character.Humanoid.WalkSpeed = value
    getgenv().Config.Player.WalkSpeed = value
end)

PlayerSection:NewSlider("🦘 JumpPower", "Lực nhảy", 500, 50, function(value)
    Character.Humanoid.JumpPower = value
    getgenv().Config.Player.JumpPower = value
end)

PlayerSection:NewToggle("👻 No Clip", "Đi xuyên tường", function(state)
    getgenv().Config.Player.NoClip = state
    if state then spawn(function()
        while getgenv().Config.Player.NoClip do wait()
            pcall(function()
                for _, part in pairs(Character:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            end)
        end
    end) end
end)

PlayerSection:NewToggle("🕊️ Fly", "Bay lượn", function(state)
    getgenv().FlyEnabled = state
    if state then loadstring(game:HttpGet("https://raw.githubusercontent.com/Xi0Nx/Fly/main/Fly.lua"))() end
end)

-- ✨ ABILITIES TAB
local AbilityTab = Window:NewTab("✨ Abilities")
local AbilitySection = AbilityTab:NewSection("Auto Abilities")

AbilitySection:NewToggle("🛡️ Auto Buso Haki", "Tự động bật Buso", function(state)
    getgenv().Config.Abilities.AutoBuso = state
    if state then spawn(function()
        while getgenv().Config.Abilities.AutoBuso do wait(8)
            pcall(function() game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Buso") end)
        end
    end) end
end)

AbilitySection:NewToggle("🌀 Auto Ken Haki", "Tự động bật Ken", function(state)
    getgenv().Config.Abilities.AutoKen = state
end)

AbilitySection:NewToggle("🌟 Auto Race", "Tự động nâng cấp Race", function(state)
    getgenv().AutoRace = state
end)

-- 🌍 TELEPORT TAB
local TeleportTab = Window:NewTab("🌍 Teleport")
local TeleportSection = TeleportTab:NewSection("Teleport Locations")

local Locations = {
    ["🏝️ Starter Island"] = CFrame.new(100, 50, 100),
    ["⚓ Marine Base"] = CFrame.new(-2850, 322, 5300),
    ["🌴 Jungle"] = CFrame.new(-1500, 60, 100),
    ["🏔️ Snow Mountain"] = CFrame.new(1200, 60, -1300),
    ["☁️ Sky Island"] = CFrame.new(-4600, 800, -2600),
    ["🔱 Third Sea"] = CFrame.new(-6500, 200, -4500)
}

for name, cf in pairs(Locations) do
    TeleportSection:NewButton(name, "Teleport tới " .. name, function()
        Character.HumanoidRootPart.CFrame = cf
    end)
end

-- 🔧 MISC TAB
local MiscTab = Window:NewTab("🔧 Misc")
local MiscSection = MiscTab:NewSection("Miscellaneous")

MiscSection:NewToggle("🎁 Auto Redeem Codes", "Tự động nhập code", function(state)
    if state then for _, code in pairs({"KITT_RESET", "SUB2GAMERROBOT_EXP1", "StrawHatMaine"}) do
        pcall(function() game:GetService("ReplicatedStorage").Remotes.Redeem:InvokeServer(code) end)
    end end
end)

MiscSection:NewButton("🔄 Rejoin Server", "Rejoin server", function()
    game:GetService("TeleportService"):Teleport(game.PlaceId)
end)

MiscSection:NewButton("🚀 Server Hop", "Đổi server", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/FilteringEnabled/FE-Scripts/main/AutoServerHop"))()
end)

MiscSection:NewKeybind("🔑 Toggle GUI", "Ẩn/hiện GUI", Enum.KeyCode.RightControl, function()
    Library:ToggleUI()
end)

-- FINAL NOTIFICATION
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "🌟 TIẾN HUB ULTRA PREMIUM",
    Text = "Bản ngắn gọn đã sẵn sàng!",
    Duration = 5
})

print("🌟 TIẾN HUB ULTRA PREMIUM - Short Version Loaded!")