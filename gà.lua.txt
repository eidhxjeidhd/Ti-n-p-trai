-- Gà Gaming Hub - Blox Fruits Script
-- Simple but Professional UI
-- Created by Gà Gaming

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local localPlayer = Players.LocalPlayer

-- Configuration
getgenv().GaGaming = {
    AutoFarm = false,
    AutoTeleport = false,
    AutoNewWorld = false,
    AutoBoss = false,
    AutoSword = false,
    AutoBuyFruits = false,
    AutoFarmBone = false,
    AutoCollectDrops = false
}

-- Tạo GUI đơn giản
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local TabButtons = Instance.new("Frame")
local ContentFrame = Instance.new("Frame")

ScreenGui.Name = "GaGamingHub"
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Main Frame
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.3, 0, 0.2, 0)
MainFrame.Size = UDim2.new(0, 600, 0, 400)
MainFrame.Active = true
MainFrame.Draggable = true

-- Header
local Header = Instance.new("Frame")
Header.Name = "Header"
Header.Parent = MainFrame
Header.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
Header.BorderSizePixel = 0
Header.Size = UDim2.new(1, 0, 0, 50)

local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = Header
Title.BackgroundTransparency = 1
Title.Size = UDim2.new(1, 0, 1, 0)
Title.Font = Enum.Font.GothamBold
Title.Text = "🐔 Gà Gaming Hub v1.0 | Blox Fruits"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 18

local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Parent = Header
CloseButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
CloseButton.BorderSizePixel = 0
CloseButton.Position = UDim2.new(0.95, 0, 0.2, 0)
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 14

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Tab Buttons
TabButtons.Name = "TabButtons"
TabButtons.Parent = MainFrame
TabButtons.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
TabButtons.BorderSizePixel = 0
TabButtons.Position = UDim2.new(0, 0, 0.125, 0)
TabButtons.Size = UDim2.new(0, 150, 0, 350)

-- Content Frame
ContentFrame.Name = "ContentFrame"
ContentFrame.Parent = MainFrame
ContentFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
ContentFrame.BorderSizePixel = 0
ContentFrame.Position = UDim2.new(0.25, 0, 0.125, 0)
ContentFrame.Size = UDim2.new(0, 450, 0, 350)

-- Tạo các tab
local Tabs = {
    Main = {Name = "🏠 Main", Content = {}},
    Farming = {Name = "⚔️ Farming", Content = {}},
    Boss = {Name = "👑 Boss", Content = {}},
    Sword = {Name = "⚔️ Sword", Content = {}},
    Player = {Name = "👤 Player", Content = {}}
}

local currentTab = "Main"

-- Tạo nút tab
local function createTabButton(tabName, position)
    local button = Instance.new("TextButton")
    button.Name = tabName .. "Tab"
    button.Parent = TabButtons
    button.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
    button.BorderSizePixel = 0
    button.Position = position
    button.Size = UDim2.new(0.9, 0, 0, 40)
    button.Font = Enum.Font.Gotham
    button.Text = Tabs[tabName].Name
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    button.TextSize = 14
    
    button.MouseButton1Click:Connect(function()
        currentTab = tabName
        updateContent()
    end)
    
    return button
end

-- Tạo toggle button
local function createToggle(text, configName, position, parent)
    local toggleFrame = Instance.new("Frame")
    toggleFrame.Name = text .. "Toggle"
    toggleFrame.Parent = parent
    toggleFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
    toggleFrame.BorderSizePixel = 0
    toggleFrame.Position = position
    toggleFrame.Size = UDim2.new(1, -20, 0, 35)
    
    local toggleText = Instance.new("TextLabel")
    toggleText.Name = "Text"
    toggleText.Parent = toggleFrame
    toggleText.BackgroundTransparency = 1
    toggleText.Position = UDim2.new(0.05, 0, 0, 0)
    toggleText.Size = UDim2.new(0.7, 0, 1, 0)
    toggleText.Font = Enum.Font.Gotham
    toggleText.Text = text
    toggleText.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleText.TextSize = 14
    toggleText.TextXAlignment = Enum.TextXAlignment.Left
    
    local toggleButton = Instance.new("TextButton")
    toggleButton.Name = "Toggle"
    toggleButton.Parent = toggleFrame
    toggleButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    toggleButton.BorderSizePixel = 0
    toggleButton.Position = UDim2.new(0.85, 0, 0.2, 0)
    toggleButton.Size = UDim2.new(0, 40, 0, 20)
    toggleButton.Font = Enum.Font.GothamBold
    toggleButton.Text = "OFF"
    toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleButton.TextSize = 10
    
    toggleButton.MouseButton1Click:Connect(function()
        getgenv().GaGaming[configName] = not getgenv().GaGaming[configName]
        if getgenv().GaGaming[configName] then
            toggleButton.BackgroundColor3 = Color3.fromRGB(50, 255, 50)
            toggleButton.Text = "ON"
            print("✅ " .. text .. " activated!")
        else
            toggleButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
            toggleButton.Text = "OFF"
            print("❌ " .. text .. " deactivated!")
        end
    end)
    
    return toggleFrame
end

-- Tạo nội dung cho các tab
local function createMainTab()
    local section = Instance.new("Frame")
    section.Name = "MainSection"
    section.Parent = ContentFrame
    section.BackgroundTransparency = 1
    section.Size = UDim2.new(1, 0, 1, 0)
    
    local sectionTitle = Instance.new("TextLabel")
    sectionTitle.Name = "SectionTitle"
    sectionTitle.Parent = section
    sectionTitle.BackgroundTransparency = 1
    sectionTitle.Position = UDim2.new(0, 10, 0, 10)
    sectionTitle.Size = UDim2.new(1, -20, 0, 30)
    sectionTitle.Font = Enum.Font.GothamBold
    sectionTitle.Text = "🏠 MAIN FEATURES"
    sectionTitle.TextColor3 = Color3.fromRGB(255, 215, 0)
    sectionTitle.TextSize = 16
    sectionTitle.TextXAlignment = Enum.TextXAlignment.Left
    
    -- Các toggle cho Main tab
    createToggle("Auto Farm", "AutoFarm", UDim2.new(0, 10, 0, 50), section)
    createToggle("Auto Teleport", "AutoTeleport", UDim2.new(0, 10, 0, 95), section)
    createToggle("Auto Collect Drops", "AutoCollectDrops", UDim2.new(0, 10, 0, 140), section)
    createToggle("Auto Buy Fruits", "AutoBuyFruits", UDim2.new(0, 10, 0, 185), section)
    createToggle("Auto New World", "AutoNewWorld", UDim2.new(0, 10, 0, 230), section)
end

local function createFarmingTab()
    local section = Instance.new("Frame")
    section.Name = "FarmingSection"
    section.Parent = ContentFrame
    section.BackgroundTransparency = 1
    section.Size = UDim2.new(1, 0, 1, 0)
    section.Visible = false
    
    local sectionTitle = Instance.new("TextLabel")
    sectionTitle.Name = "SectionTitle"
    sectionTitle.Parent = section
    sectionTitle.BackgroundTransparency = 1
    sectionTitle.Position = UDim2.new(0, 10, 0, 10)
    sectionTitle.Size = UDim2.new(1, -20, 0, 30)
    sectionTitle.Font = Enum.Font.GothamBold
    sectionTitle.Text = "⚔️ FARMING FEATURES"
    sectionTitle.TextColor3 = Color3.fromRGB(255, 215, 0)
    sectionTitle.TextSize = 16
    sectionTitle.TextXAlignment = Enum.TextXAlignment.Left
    
    createToggle("Auto Farm Bone", "AutoFarmBone", UDim2.new(0, 10, 0, 50), section)
end

local function createBossTab()
    local section = Instance.new("Frame")
    section.Name = "BossSection"
    section.Parent = ContentFrame
    section.BackgroundTransparency = 1
    section.Size = UDim2.new(1, 0, 1, 0)
    section.Visible = false
    
    local sectionTitle = Instance.new("TextLabel")
    sectionTitle.Name = "SectionTitle"
    sectionTitle.Parent = section
    sectionTitle.BackgroundTransparency = 1
    sectionTitle.Position = UDim2.new(0, 10, 0, 10)
    sectionTitle.Size = UDim2.new(1, -20, 0, 30)
    sectionTitle.Font = Enum.Font.GothamBold
    sectionTitle.Text = "👑 BOSS FEATURES"
    sectionTitle.TextColor3 = Color3.fromRGB(255, 215, 0)
    sectionTitle.TextSize = 16
    sectionTitle.TextXAlignment = Enum.TextXAlignment.Left
    
    createToggle("Auto Boss", "AutoBoss", UDim2.new(0, 10, 0, 50), section)
end

local function createSwordTab()
    local section = Instance.new("Frame")
    section.Name = "SwordSection"
    section.Parent = ContentFrame
    section.BackgroundTransparency = 1
    section.Size = UDim2.new(1, 0, 1, 0)
    section.Visible = false
    
    local sectionTitle = Instance.new("TextLabel")
    sectionTitle.Name = "SectionTitle"
    sectionTitle.Parent = section
    sectionTitle.BackgroundTransparency = 1
    sectionTitle.Position = UDim2.new(0, 10, 0, 10)
    sectionTitle.Size = UDim2.new(1, -20, 0, 30)
    sectionTitle.Font = Enum.Font.GothamBold
    sectionTitle.Text = "⚔️ SWORD FEATURES"
    sectionTitle.TextColor3 = Color3.fromRGB(255, 215, 0)
    sectionTitle.TextSize = 16
    sectionTitle.TextXAlignment = Enum.TextXAlignment.Left
    
    createToggle("Auto Sword", "AutoSword", UDim2.new(0, 10, 0, 50), section)
end

local function createPlayerTab()
    local section = Instance.new("Frame")
    section.Name = "PlayerSection"
    section.Parent = ContentFrame
    section.BackgroundTransparency = 1
    section.Size = UDim2.new(1, 0, 1, 0)
    section.Visible = false
    
    local sectionTitle = Instance.new("TextLabel")
    sectionTitle.Name = "SectionTitle"
    sectionTitle.Parent = section
    sectionTitle.BackgroundTransparency = 1
    sectionTitle.Position = UDim2.new(0, 10, 0, 10)
    sectionTitle.Size = UDim2.new(1, -20, 0, 30)
    sectionTitle.Font = Enum.Font.GothamBold
    sectionTitle.Text = "👤 PLAYER FEATURES"
    sectionTitle.TextColor3 = Color3.fromRGB(255, 215, 0)
    sectionTitle.TextSize = 16
    sectionTitle.TextXAlignment = Enum.TextXAlignment.Left
end

-- Cập nhật nội dung tab
local function updateContent()
    for tabName, tabData in pairs(Tabs) do
        if tabData.Content then
            tabData.Content.Visible = (tabName == currentTab)
        end
    end
end

-- Tạo các tab
local tabPositions = {
    Main = UDim2.new(0.05, 0, 0.05, 0),
    Farming = UDim2.new(0.05, 0, 0.15, 0),
    Boss = UDim2.new(0.05, 0, 0.25, 0),
    Sword = UDim2.new(0.05, 0, 0.35, 0),
    Player = UDim2.new(0.05, 0, 0.45, 0)
}

for tabName, position in pairs(tabPositions) do
    createTabButton(tabName, position)
end

-- Tạo nội dung
Tabs.Main.Content = createMainTab()
Tabs.Farming.Content = createFarmingTab()
Tabs.Boss.Content = createBossTab()
Tabs.Sword.Content = createSwordTab()
Tabs.Player.Content = createPlayerTab()

-- Hiển thị tab mặc định
updateContent()

-- Auto Farm Function
spawn(function()
    while wait(1) do
        if getgenv().GaGaming.AutoFarm then
            -- Code auto farm ở đây
            print("🤖 Auto Farm đang chạy...")
        end
    end
end)