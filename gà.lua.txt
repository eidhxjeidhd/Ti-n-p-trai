-- Blox Fruits Ultimate Hub
-- Tổng hợp tính năng từ Redz Hub, Banana Hub, Maru Hub
-- Script chỉ dành cho mục đích giáo dục

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("Blox Fruits Ultimate Hub", "DarkTheme")

-- MAIN TAB
local Main = Window:NewTab("Main")
local MainSection = Main:NewSection("Main Features")

MainSection:NewButton("Auto Farm Level", "Tự động farm level", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xQuartyx/DonateMe/main/ScriptLoader"))()
end)

MainSection:NewButton("Auto Farm Boss", "Tự động farm boss", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/ic3w0lf22/Unnamed-ESP/master/UnnamedESP.lua"))()
end)

MainSection:NewButton("Auto Sea Events", "Tự động sự kiện biển", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
end)

-- TELEPORT TAB
local Teleport = Window:NewTab("Teleport")
local TeleportSection = Teleport:NewSection("Dịch chuyển")

TeleportSection:NewButton("Teleport đến Island", "Dịch chuyển đến đảo", function()
    local plr = game.Players.LocalPlayer
    plr.Character.HumanoidRootPart.CFrame = CFrame.new(0, 50, 0) -- Thay đổi tọa độ
end)

TeleportSection:NewDropdown("Chọn đảo", "Danh sách các đảo", 
    {"Starter Island", "Jungle", "Pirate Village", "Desert", "Snow Mountain", "Marine Fortress"}, 
    function(currentOption)
    -- Code teleport đến đảo được chọn
end)

-- FRUITS TAB
local Fruits = Window:NewTab("Fruits")
local FruitsSection = Fruits:NewSection("Trái ác quỷ")

FruitsSection:NewButton("Mua trái ngẫu nhiên", "Mua trái ác quỷ ngẫu nhiên", function()
    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Cousin","Buy")
end)

FruitsSection:NewToggle("Auto Store Fruits", "Tự động lưu trữ trái", function(state)
    getgenv().AutoStoreFruits = state
    while getgenv().AutoStoreFruits do
        wait(1)
        -- Code auto store fruits
    end
end)

FruitsSection:NewButton("Tìm trái trên đất", "Hiển thị trái ác quỷ trên mặt đất", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xQuartyx/DonateMe/main/ScriptLoader"))()
end)

-- RAID TAB
local Raid = Window:NewTab("Raid")
local RaidSection = Raid:NewSection("Raid Features")

RaidSection:NewButton("Tự động Raid", "Tự động tham gia raid", function()
    getgenv().AutoRaid = true
    while getgenv().AutoRaid do
        wait(1)
        -- Code auto raid
    end
end)

RaidSection:NewDropdown("Chọn loại Raid", "Chọn loại raid muốn tham gia", 
    {"Flame", "Ice", "Quake", "Light", "Dark", "String", "Rumble", "Magma", "Human: Buddha", "Sand", "Bird: Phoenix"}, 
    function(currentOption)
    getgenv().SelectedRaid = currentOption
end)

-- ESP TAB
local ESP = Window:NewTab("ESP")
local ESPSection = ESP:NewSection("ESP Features")

ESPSection:NewToggle("ESP Trái ác quỷ", "Hiển thị trái ác quỷ", function(state)
    getgenv().FruitESP = state
    -- Code ESP trái ác quỷ
end)

ESPSection:NewToggle("ESP Chest", "Hiển thị rương", function(state)
    getgenv().ChestESP = state
    -- Code ESP rương
end)

ESPSection:NewToggle("ESP Boss", "Hiển thị boss", function(state)
    getgenv().BossESP = state
    -- Code ESP boss
end)

-- PLAYER TAB
local Player = Window:NewTab("Player")
local PlayerSection = Player:NewSection("Player Modifications")

PlayerSection:NewSlider("WalkSpeed", "Tốc độ di chuyển", 500, 16, function(s)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = s
end)

PlayerSection:NewSlider("JumpPower", "Sức nhảy", 350, 50, function(s)
    game.Players.LocalPlayer.Character.Humanoid.JumpPower = s
end)

PlayerSection:NewButton("Bay (X)", "Bật/tắt bay (Nhấn X)", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGui/main/FlyGui.lua"))()
end)

-- MISC TAB
local Misc = Window:NewTab("Misc")
local MiscSection = Misc:NewSection("Miscellaneous")

MiscSection:NewButton("Auto Điều khiển tàu", "Tự động điều khiển tàu", function()
    getgenv().AutoSail = true
    while getgenv().AutoSail do
        wait(1)
        -- Code auto sail
    end
end)

MiscSection:NewButton("Auto Nhặt tiền", "Tự động nhặt tiền Beli", function()
    getgenv().AutoCollectBeli = true
    while getgenv().AutoCollectBeli do
        wait(1)
        -- Code auto collect beli
    end
end)

MiscSection:NewButton("Mở tất cả cửa", "Mở tất cả cửa trên bản đồ", function()
    for i,v in pairs(workspace:GetDescendants()) do
        if v.Name == "Door" then
            v:Destroy()
        end
    end
end)

-- SETTINGS TAB
local Settings = Window:NewTab("Settings")
local SettingsSection = Settings:NewSection("Cài đặt")

SettingsSection:NewKeybind("Ẩn/Hiện GUI", "Phím tắt ẩn/hiện GUI", Enum.KeyCode.RightControl, function()
	Library:ToggleUI()
end)

SettingsSection:NewButton("Destroy GUI", "Xóa GUI", function()
    Library:Destroy()
end)

-- Thông báo khi load xong
game:GetService("StarterGui"):SetCore("SendNotification",{
    Title = "Blox Fruits Ultimate Hub",
    Text = "Đã load thành công!",
    Duration = 5
})