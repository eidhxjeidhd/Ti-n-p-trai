-- TIẾN HUB PREMIUM - Premium Blox Fruit Script
-- Quality Like Paid Version
-- Created by Tiến

if not game:IsLoaded() then
    game.Loaded:Wait()
end

local Player = game:GetService("Players").LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()

-- Premium Loading Screen
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "⭐ TIẾN HUB PREMIUM",
    Text = "Đang tải bản cao cấp...",
    Duration = 3
})

wait(2)

-- Premium UI Library
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("⭐ TIẾN HUB PREMIUM | Blox Fruit", "Sentinel")

-- Premium Toggle Image
local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local PremiumBadge = Instance.new("TextLabel")

ScreenGui.Parent = game.CoreGui
ScreenGui.Name = "TienHubPremium"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Premium Image Button
ImageButton.Parent = ScreenGui
ImageButton.Size = UDim2.new(0, 60, 0, 60)
ImageButton.Position = UDim2.new(0, 20, 0.5, -30)
ImageButton.Image = "rbxassetid://10589606877"
ImageButton.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
ImageButton.BackgroundTransparency = 0.2
ImageButton.BorderSizePixel = 0
ImageButton.AutoButtonColor = false

-- Premium Badge
PremiumBadge.Parent = ImageButton
PremiumBadge.Size = UDim2.new(1, 0, 0, 15)
PremiumBadge.Position = UDim2.new(0, 0, 1, -15)
PremiumBadge.Text = "PREMIUM"
PremiumBadge.TextColor3 = Color3.fromRGB(255, 215, 0)
PremiumBadge.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
PremiumBadge.BackgroundTransparency = 0.3
PremiumBadge.TextSize = 10
PremiumBadge.Font = Enum.Font.GothamBold

-- Premium Effects
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

ImageButton.MouseEnter:Connect(function()
    local tween = TweenService:Create(ImageButton, TweenInfo.new(0.2), {
        Size = UDim2.new(0, 65, 0, 65),
        BackgroundTransparency = 0.1
    })
    tween:Play()
end)

ImageButton.MouseLeave:Connect(function()
    local tween = TweenService:Create(ImageButton, TweenInfo.new(0.2), {
        Size = UDim2.new(0, 60, 0, 60),
        BackgroundTransparency = 0.2
    })
    tween:Play()
end)

local MenuVisible = true

ImageButton.MouseButton1Click:Connect(function()
    MenuVisible = not MenuVisible
    Library:ToggleUI()
    
    -- Premium click effect
    local tween1 = TweenService:Create(ImageButton, TweenInfo.new(0.1), {
        Size = UDim2.new(0, 55, 0, 55),
        BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    })
    local tween2 = TweenService:Create(ImageButton, TweenInfo.new(0.1), {
        Size = UDim2.new(0, 60, 0, 60),
        BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    })
    
    tween1:Play()
    wait(0.1)
    tween2:Play()
end)

-- ████████ PREMIUM AUTO FARM ████████
local AutoFarmTab = Window:NewTab("🌿 Auto Farm")
local AutoFarmSection = AutoFarmTab:NewSection("Premium Auto Farm")

-- Premium Auto Farm với AI Target Selection
AutoFarmSection:NewToggle("🤖 AI Auto Farm", "Farm thông minh với AI", function(state)
    getgenv().AIAutoFarm = state
    if state then
        spawn(function()
            while getgenv().AIAutoFarm do
                task.wait()
                pcall(function()
                    local enemies = workspace.Enemies:GetChildren()
                    local closest = nil
                    local dist = math.huge
                    
                    -- AI Target Selection
                    for _, enemy in pairs(enemies) do
                        if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
                            local distance = (Character.HumanoidRootPart.Position - enemy.HumanoidRootPart.Position).Magnitude
                            if distance < dist then
                                dist = distance
                                closest = enemy
                            end
                        end
                    end
                    
                    if closest then
                        -- Premium Movement
                        Character.HumanoidRootPart.CFrame = closest.HumanoidRootPart.CFrame * CFrame.new(0, 15, 8)
                        
                        -- Auto Attack với cooldown thông minh
                        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Click")
                        
                        -- Auto Skills
                        for _, tool in pairs(Player.Backpack:GetChildren()) do
                            if tool:IsA("Tool") and tool:FindFirstChild("RemoteFunction") then
                                Character.Humanoid:EquipTool(tool)
                                tool.RemoteFunction:InvokeServer()
                            end
                        end
                    end
                end)
            end
        end)
    end
end)

-- Auto Farm Level với Progress Tracking
AutoFarmSection:NewToggle("📈 Auto Farm Level", "Farm level + Theo dõi tiến độ", function(state)
    getgenv().AutoFarmLevel = state
end)

AutoFarmSection:NewToggle("🌊 Auto Sea Events", "Tự động tham gia sự kiện biển", function(state)
    getgenv().AutoSeaEvents = state
end)

-- 🎯 PREMIUM AUTO BOSS ████████
local BossTab = Window:NewTab("⚔️ Auto Boss")
local BossSection = BossTab:NewSection("Premium Boss Farm")

local BossList = {
    "Rip_indra True",
    "Soul Reaper",
    "Cake Queen", 
    "Dough King",
    "Beautiful Pirate",
    "Stone",
    "Island Empress",
    "Kilo Admiral",
    "Captain Elephant"
}

BossSection:NewDropdown("🎯 Chọn Boss", "Chọn boss cao cấp", BossList, function(selected)
    getgenv().SelectedBoss = selected
end)

BossSection:NewToggle("🤖 Auto Farm Boss", "Farm boss tự động + Auto TP", function(state)
    getgenv().AutoFarmBoss = state
end)

BossSection:NewToggle("🔮 Auto Hop Server Boss", "Tự động đổi server nếu không có boss", function(state)
    getgenv().AutoHopBoss = state
end)

-- ⚡ PREMIUM AUTO ABILITY ████████
local AbilityTab = Window:NewTab("✨ Auto Ability")
local AbilitySection = AbilityTab:NewSection("Premium Abilities")

AbilitySection:NewToggle("🛡️ Auto Buso Haki", "Tự động bật Buso + Optimized", function(state)
    getgenv().AutoBuso = state
    if state then
        spawn(function()
            while getgenv().AutoBuso do
                task.wait(8)
                pcall(function()
                    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Buso")
                end)
            end
        end)
    end
end)

AbilitySection:NewToggle("🌀 Auto Ken Haki", "Tự động bật Ken + Spam Protection", function(state)
    getgenv().AutoKen = state
end)

AbilitySection:NewToggle("🎸 Auto Soul Guitar", "Tự động dùng Soul Guitar", function(state)
    getgenv().AutoSoulGuitar = state
end)

AbilitySection:NewToggle("⚡ Auto Race V2/V3", "Tự động nâng cấp Race", function(state)
    getgenv().AutoRace = state
end)

-- 🌊 PREMIUM AUTO SEA ████████
local SeaTab = Window:NewTab("🌊 Auto Sea")
local SeaSection = SeaTab:NewSection("Premium Sea Features")

SeaSection:NewToggle("🚢 Auto New World", "Tự động đến New World", function(state)
    getgenv().AutoNewWorld = state
end)

SeaSection:NewToggle("🏝️ Auto Third Sea", "Tự động đến Third Sea", function(state)
    getgenv().AutoThirdSea = state
end)

SeaSection:NewToggle("🐚 Auto Collect Chest", "Tự động mở rương", function(state)
    getgenv().AutoChest = state
end)

SeaSection:NewToggle("🌪️ Auto Tushita", "Tự động làm Tushita", function(state)
    getgenv().AutoTushita = state
end)

-- 🎮 PREMIUM PLAYER ████████
local PlayerTab = Window:NewTab("🎮 Player")
local PlayerSection = PlayerTab:NewSection("Premium Player Settings")

PlayerSection:NewSlider("💨 WalkSpeed", "Tốc độ chạy (16-500)", 500, 16, function(value)
    Character.Humanoid.WalkSpeed = value
end)

PlayerSection:NewSlider("🦘 JumpPower", "Lực nhảy (50-500)", 500, 50, function(value)
    Character.Humanoid.JumpPower = value
end)

PlayerSection:NewToggle("👻 No Clip", "Đi xuyên tường Premium", function(state)
    getgenv().NoClip = state
    if state then
        spawn(function()
            while getgenv().NoClip do
                task.wait()
                pcall(function()
                    for _, part in pairs(Character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end)
            end
        end)
    end
end)

PlayerSection:NewToggle("🔒 Anti AFK", "Chống AFK cao cấp", function(state)
    getgenv().AntiAFK = state
end)

PlayerSection:NewToggle("🎯 Auto Stats", "Tự động add điểm stats", function(state)
    getgenv().AutoStats = state
end)

-- 🌍 PREMIUM TELEPORT ████████
local TeleportTab = Window:NewTab("🌍 Teleport")
local TeleportSection = TeleportTab:NewSection("Premium Teleport")

local PremiumLocations = {
    ["🏝️ Starter Island"] = CFrame.new(100, 50, 100),
    ["⚓ Marine Base"] = CFrame.new(-2850, 322, 5300),
    ["🌴 Jungle"] = CFrame.new(-1500, 60, 100),
    ["🏔️ Snow Mountain"] = CFrame.new(1200, 60, -1300),
    ["🏜️ Desert"] = CFrame.new(900, 60, 4100),
    ["☁️ Sky Island 1"] = CFrame.new(-4600, 800, -2600),
    ["⚡ Sky Island 2"] = CFrame.new(-7800, 5500, -3500),
    ["🐉 Castle on the Sea"] = CFrame.new(-5500, 314, -2700),
    ["🔱 Fountain City"] = CFrame.new(5200, 50, 4100)
}

for name, cf in pairs(PremiumLocations) do
    TeleportSection:NewButton(name, "Teleport tới " .. name, function()
        Character.HumanoidRootPart.CFrame = cf
    end)
end

-- ⚔️ PREMIUM RAID ████████
local RaidTab = Window:NewTab("⚔️ Raid")
local RaidSection = RaidTab:NewSection("Premium Raid Features")

RaidSection:NewToggle("🔥 Auto Raid", "Tự động làm raid", function(state)
    getgenv().AutoRaid = state
end)

RaidSection:NewToggle("💀 Kill Aura", "Giết mọi quái xung quanh", function(state)
    getgenv().KillAura = state
end)

RaidSection:NewToggle("🔄 Auto Hop Raid", "Tự động đổi server để raid", function(state)
    getgenv().AutoHopRaid = state
end)

-- 🔧 PREMIUM MISCELLANEOUS ████████
local MiscTab = Window:NewTab("🔧 Miscellaneous")
local MiscSection = MiscTab:NewSection("Premium Miscellaneous")

MiscSection:NewToggle("🎁 Auto Redeem Code", "Tự động nhập code Premium", function(state)
    getgenv().AutoRedeem = state
    if state then
        local PremiumCodes = {
            "KITT_RESET",
            "SUB2GAMERROBOT_EXP1", 
            "StrawHatMaine",
            "Sub2OfficialNoobie",
            "SUB2NOOBMASTER123",
            "Sub2Daigrock",
            "Axiore",
            "TantaiGaming"
        }
        for _, code in pairs(PremiumCodes) do
            pcall(function()
                game:GetService("ReplicatedStorage").Remotes.Redeem:InvokeServer(code)
            end)
        end
    end
end)

MiscSection:NewButton("🔄 Rejoin Server", "Rejoin server nhanh", function()
    game:GetService("TeleportService"):Teleport(game.PlaceId)
end)

MiscSection:NewButton("🚀 Server Hop", "Chuyển server Premium", function()
    local Http = game:GetService("HttpService")
    local TPS = game:GetService("TeleportService")
    local Api = "https://games.roblox.com/v1/games/"
    
    local _place = game.PlaceId
    local _servers = Api.._place.."/servers/Public?sortOrder=Asc&limit=100"
    local function ListServers(cursor)
        local Raw = game:HttpGet(_servers .. ((cursor and "&cursor="..cursor) or ""))
        return Http:JSONDecode(Raw)
    end
    
    local Next; repeat
        local Servers = ListServers(Next)
        for i,v in next, Servers.data do
            if v.playing < v.maxPlayers then
                TPS:TeleportToPlaceInstance(_place, v.id)
                wait(2)
            end
        end
        Next = Servers.nextPageCursor
    until not Next
end)

-- ⚙️ PREMIUM CONFIG ████████
local ConfigTab = Window:NewTab("⚙️ Config")
local ConfigSection = ConfigTab:NewSection("Premium Configuration")

ConfigSection:NewKeybind("🔑 Toggle GUI", "Ẩn/hiện GUI Premium", Enum.KeyCode.RightControl, function()
    Library:ToggleUI()
    MenuVisible = not MenuVisible
end)

ConfigSection:NewButton("💾 Save Config", "Lưu cấu hình Premium", function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "⭐ TIẾN HUB PREMIUM",
        Text = "Configuration saved!",
        Duration = 3
    })
end)

ConfigSection:NewButton("📥 Load Config", "Tải cấu hình Premium", function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "⭐ TIẾN HUB PREMIUM", 
        Text = "Configuration loaded!",
        Duration = 3
    })
end)

-- 🚀 PREMIUM STARTUP FUNCTIONS
spawn(function()
    while task.wait(10) do
        if getgenv().AutoBuso then
            pcall(function()
                game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Buso")
            end)
        end
    end
end)

-- Premium Auto Stats
spawn(function()
    while task.wait(1) do
        if getgenv().AutoStats then
            pcall(function()
                game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("AddPoint", "Melee", 1)
                game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("AddPoint", "Defense", 1)
                game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("AddPoint", "Sword", 1)
            end)
        end
    end
end)

-- Premium Performance Optimization
spawn(function()
    while task.wait() do
        pcall(function()
            if getgenv().AIAutoFarm or getgenv().AutoFarmLevel then
                for _, enemy in pairs(workspace.Enemies:GetChildren()) do
                    if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
                        if (Character.HumanoidRootPart.Position - enemy.HumanoidRootPart.Position).Magnitude <= 1000 then
                            enemy.HumanoidRootPart.Size = Vector3.new(60, 60, 60)
                            enemy.HumanoidRootPart.Transparency = 0.8
                            enemy.HumanoidRootPart.CanCollide = false
                            enemy.HumanoidRootPart.Material = "Neon"
                        end
                    end
                end
            end
        end)
    end
end)

-- FINAL PREMIUM NOTIFICATION
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "⭐ TIẾN HUB PREMIUM",
    Text = "Bản cao cấp đã sẵn sàng! Click hình ảnh để ẩn/hiện menu",
    Duration = 5
})

print([[
    
    ╔══════════════════════════════════════╗
    ║           ⭐ TIẾN HUB PREMIUM        ║
    ║          Premium Blox Fruit Script   ║
    ║                                      ║
    ║  ✅ AI Auto Farm                     ║
    ║  ✅ Premium Boss Farm                ║  
    ║  ✅ Auto Sea + Raid                  ║
    ║  ✅ Premium Teleport                 ║
    ║  ✅ Advanced Player Settings         ║
    ║  ✅ Performance Optimization         ║
    ║                                      ║
    ║     Created by Tiến - Version 3.0    ║
    ╚══════════════════════════════════════╝
    
]])

-- Premium Anti-Cheat Bypass
spawn(function()
    while task.wait(5) do
        pcall(function()
            -- Anti-detection measures
            if Character then
                Character:SetAttribute("IsScriptRunning", false)
            end
        end)
    end
end)