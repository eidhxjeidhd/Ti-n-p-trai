-- Gà Gaming Hub - WITH LOGO & TOGGLE MENU
-- Logo từ ID của em + Ẩn/hiện menu bằng logo

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local VirtualInput = game:GetService("VirtualInputManager")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local localPlayer = Players.LocalPlayer
local Remotes = ReplicatedStorage.Remotes

-- ID LOGO CỦA EM
local LOGO_ID = "84957548476668"

-- CONFIG TẤT CẢ TÍNH NĂNG
local Config = {
    AutoFarm = false,
    FastPunch = false,
    LongRange = false,
    HighPosition = false,
    BestLocation = false,
    PlayerTeleport = false,
    FruitFinder = false,
    FruitShop = false,
    IslandTeleport = false,
    AllFeatures = false,
    MenuVisible = true
}

-- TẠO GUI VỚI LOGO
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GaGamingHubWithLogo"
ScreenGui.Parent = localPlayer:WaitForChild("PlayerGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- 🖼️ LOGO CÓ THỂ BẤM ĐỂ ẨN/HIỆN MENU
local LogoButton = Instance.new("ImageButton")
LogoButton.Name = "LogoToggle"
LogoButton.Size = UDim2.new(0, 80, 0, 80)
LogoButton.Position = UDim2.new(0, 10, 0, 10)
LogoButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
LogoButton.BackgroundTransparency = 1
LogoButton.Image = "rbxassetid://" .. LOGO_ID
LogoButton.ScaleType = Enum.ScaleType.Crop
LogoButton.Parent = ScreenGui

-- Thêm hiệu ứng cho logo
local LogoCorner = Instance.new("UICorner")
LogoCorner.CornerRadius = UDim.new(0.2, 0)
LogoCorner.Parent = LogoButton

local LogoStroke = Instance.new("UIStroke")
LogoStroke.Color = Color3.fromRGB(255, 215, 0)
LogoStroke.Thickness = 3
LogoStroke.Parent = LogoButton

-- MAIN MENU FRAME
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainMenu"
MainFrame.Size = UDim2.new(0, 350, 0, 500)
MainFrame.Position = UDim2.new(0.3, 0, 0.2, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui
MainFrame.Active = true
MainFrame.Draggable = true

-- HEADER VỚI TÊN EM
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 50)
Header.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
Header.BorderSizePixel = 0
Header.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0.6, 0)
Title.BackgroundTransparency = 1
Title.Text = "🐔 GÀ GAMING HUB"
Title.TextColor3 = Color3.fromRGB(255, 215, 0)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 18
Title.Parent = Header

local SubTitle = Instance.new("TextLabel")
SubTitle.Size = UDim2.new(1, 0, 0.4, 0)
SubTitle.Position = UDim2.new(0, 0, 0.6, 0)
SubTitle.BackgroundTransparency = 1
SubTitle.Text = "Logo: " .. game.Players.LocalPlayer.Name
SubTitle.TextColor3 = Color3.fromRGB(200, 200, 200)
SubTitle.Font = Enum.Font.Gotham
SubTitle.TextSize = 11
SubTitle.Parent = Header

local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Position = UDim2.new(0.92, 0, 0.1, 0)
CloseButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 14
CloseButton.Parent = Header

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- TẠO NÚT CHO TẤT CẢ TÍNH NĂNG
local function CreateFeatureButton(text, configKey, yPos)
    local button = Instance.new("TextButton")
    button.Size = UDim2.new(0.9, 0, 0, 35)
    button.Position = UDim2.new(0.05, 0, yPos, 0)
    button.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    button.Text = "❌ " .. text
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    button.Font = Enum.Font.GothamBold
    button.TextSize = 12
    button.Parent = MainFrame
    
    -- Thêm hiệu ứng cho nút
    local buttonCorner = Instance.new("UICorner")
    buttonCorner.CornerRadius = UDim.new(0, 5)
    buttonCorner.Parent = button
    
    local buttonStroke = Instance.new("UIStroke")
    buttonStroke.Color = Color3.fromRGB(255, 255, 255)
    buttonStroke.Thickness = 1
    buttonStroke.Parent = button
    
    button.MouseButton1Click:Connect(function()
        Config[configKey] = not Config[configKey]
        
        if Config[configKey] then
            button.BackgroundColor3 = Color3.fromRGB(50, 255, 50)
            button.Text = "✅ " .. text
            print("🎯 BẬT: " .. text)
        else
            button.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
            button.Text = "❌ " .. text
            print("🛑 TẮT: " .. text)
        end
    end)
    
    return button
end

-- TẠO TẤT CẢ NÚT TÍNH NĂNG
CreateFeatureButton("AUTO FARM", "AutoFarm", 0.12)
CreateFeatureButton("ĐẤM NHANH 10 PHÁT", "FastPunch", 0.20)
CreateFeatureButton("ĐẤM XA 10M", "LongRange", 0.28)
CreateFeatureButton("BAY CAO FARM", "HighPosition", 0.36)
CreateFeatureButton("FARM VỊ TRÍ TỐT", "BestLocation", 0.44)
CreateFeatureButton("ĐỊNH VỊ NGƯỜI CHƠI", "PlayerTeleport", 0.52)
CreateFeatureButton("ĐỊNH VỊ TRÁI ÁC QUỶ", "FruitFinder", 0.60)
CreateFeatureButton("XEM CỬA HÀNG TRÁI", "FruitShop", 0.68)
CreateFeatureButton("DỊCH CHUYỂN ĐẢO", "IslandTeleport", 0.76)
CreateFeatureButton("BẬT TẤT CẢ", "AllFeatures", 0.84)

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, 0, 0, 30)
StatusLabel.Position = UDim2.new(0, 0, 0.94, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "🔴 Chưa bật tính năng nào"
StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.TextSize = 12
StatusLabel.Parent = MainFrame

-- 🖱️ CHỨC NĂNG ẨN/HIỆN MENU BẰNG LOGO
LogoButton.MouseButton1Click:Connect(function()
    Config.MenuVisible = not Config.MenuVisible
    MainFrame.Visible = Config.MenuVisible
    
    if Config.MenuVisible then
        -- Hiệu ứng khi hiện menu
        LogoButton.ImageColor3 = Color3.fromRGB(255, 255, 255)
        print("📱 HIỆN MENU")
    else
        -- Hiệu ứng khi ẩn menu
        LogoButton.ImageColor3 = Color3.fromRGB(150, 150, 150)
        print("📱 ẨN MENU")
    end
end)

-- 🚀 AUTO FARM CAO CẤP
function AdvancedAutoFarm()
    spawn(function()
        while wait(0.3) do
            if not Config.AutoFarm then break end
            
            local target = FindNearestEnemy()
            if target then
                StatusLabel.Text = "🎯 Đang farm: " .. target.Name
                
                -- BAY LÊN CAO
                if Config.HighPosition then
                    localPlayer.Character.HumanoidRootPart.CFrame = target.HumanoidRootPart.CFrame * CFrame.new(0, 15, 0)
                end
                
                -- ĐẤM XA 10M
                if Config.LongRange then
                    localPlayer.Character.HumanoidRootPart.CFrame = target.HumanoidRootPart.CFrame * CFrame.new(0, 0, 10)
                end
                
                -- ĐẤM NHANH 10 PHÁT/5 GIÂY
                if Config.FastPunch then
                    for i = 1, 10 do
                        VirtualInput:SendKeyEvent(true, "X", false, game)
                        wait(0.05)
                        VirtualInput:SendKeyEvent(false, "X", false, game)
                        wait(0.45)
                    end
                else
                    VirtualInput:SendKeyEvent(true, "X", false, game)
                    wait(0.1)
                    VirtualInput:SendKeyEvent(false, "X", false, game)
                end
                
            else
                StatusLabel.Text = "🔍 Đang tìm quái..."
            end
        end
    end)
end

-- 🎯 FARM VỊ TRÍ TỐT NHẤT
function FarmBestLocation()
    spawn(function()
        while wait(5) do
            if not Config.BestLocation then break end
            
            local bestSpots = {
                CFrame.new(-1000, 50, 1000),
                CFrame.new(1500, 50, -800),  
                CFrame.new(-500, 50, -1500),
                CFrame.new(2000, 50, 500)
            }
            
            local randomSpot = bestSpots[math.random(1, #bestSpots)]
            localPlayer.Character.HumanoidRootPart.CFrame = randomSpot
            StatusLabel.Text = "📍 Đến vị trí farm tốt!"
        end
    end)
end

-- 👤 ĐỊNH VỊ NGƯỜI CHƠI
function TeleportToPlayer()
    spawn(function()
        while wait(3) do
            if not Config.PlayerTeleport then break end
            
            local players = Players:GetPlayers()
            for _, player in pairs(players) do
                if player ~= localPlayer and player.Character then
                    localPlayer.Character.HumanoidRootPart.CFrame = player.Character.HumanoidRootPart.CFrame
                    StatusLabel.Text = "👤 Dịch đến: " .. player.Name
                    break
                end
            end
        end
    end)
end

-- 🍎 ĐỊNH VỊ TRÁI ÁC QUỶ
function FindFruits()
    spawn(function()
        while wait(2) do
            if not Config.FruitFinder then break end
            
            for _, item in pairs(Workspace:GetChildren()) do
                if item:IsA("Tool") or item.Name == "Fruit" then
                    if item:FindFirstChild("Handle") then
                        localPlayer.Character.HumanoidRootPart.CFrame = item.Handle.CFrame
                        StatusLabel.Text = "🍎 Tìm thấy trái ác quỷ!"
                        break
                    end
                end
            end
        end
    end)
end

-- 🛒 XEM CỬA HÀNG TRÁI ÁC QUỶ
function OpenFruitShop()
    spawn(function()
        while wait(10) do
            if not Config.FruitShop then break end
            
            local fruitShop = Workspace:FindFirstChild("FruitShop")
            if fruitShop then
                localPlayer.Character.HumanoidRootPart.CFrame = fruitShop:FindFirstChildOfClass("Part").CFrame
                StatusLabel.Text = "🛒 Đến cửa hàng trái!"
            end
        end
    end)
end

-- 🏝️ DỊCH CHUYỂN ĐẢO
function TeleportIslands()
    spawn(function()
        while wait(5) do
            if not Config.IslandTeleport then break end
            
            local islands = {
                ["Jungle"] = CFrame.new(-1500, 70, 100),
                ["Desert"] = CFrame.new(1000, 70, -1200),
                ["Pirate Village"] = CFrame.new(-1200, 70, 3800),
                ["Frozen Village"] = CFrame.new(1200, 70, -3800),
                ["Marine Fortress"] = CFrame.new(-4500, 70, 2000),
                ["Sky Island 1"] = CFrame.new(0, 1500, 0)
            }
            
            local islandNames = {}
            for name, _ in pairs(islands) do
                table.insert(islandNames, name)
            end
            
            local randomIsland = islandNames[math.random(1, #islandNames)]
            localPlayer.Character.HumanoidRootPart.CFrame = islands[randomIsland]
            StatusLabel.Text = "🏝️ Đến đảo: " .. randomIsland
        end
    end)
end

-- 🎯 BẬT TẤT CẢ TÍNH NĂNG
function EnableAllFeatures()
    if Config.AllFeatures then
        Config.AutoFarm = true
        Config.FastPunch = true
        Config.LongRange = true
        Config.HighPosition = true
        Config.BestLocation = true
        Config.PlayerTeleport = true
        Config.FruitFinder = true
        Config.FruitShop = true
        Config.IslandTeleport = true
        
        StatusLabel.Text = "🚀 ĐÃ BẬT TẤT CẢ TÍNH NĂNG!"
        print("🎯 ĐÃ KÍCH HOẠT TẤT CẢ TÍNH NĂNG!")
    end
end

-- TÌM QUÁI GẦN NHẤT
function FindNearestEnemy()
    local nearest = nil
    local distance = math.huge
    
    if Workspace:FindFirstChild("Enemies") then
        for _, enemy in pairs(Workspace.Enemies:GetChildren()) do
            if enemy:FindFirstChild("HumanoidRootPart") and enemy:FindFirstChild("Humanoid") then
                if enemy.Humanoid.Health > 0 then
                    local dist = (localPlayer.Character.HumanoidRootPart.Position - enemy.HumanoidRootPart.Position).Magnitude
                    if dist < distance then
                        distance = dist
                        nearest = enemy
                    end
                end
            end
        end
    end
    
    return nearest
end

-- 🔄 CHẠY TẤT CẢ TÍNH NĂNG
spawn(function()
    while wait(1) do
        if Config.AutoFarm then AdvancedAutoFarm() end
        if Config.BestLocation then FarmBestLocation() end
        if Config.PlayerTeleport then TeleportToPlayer() end
        if Config.FruitFinder then FindFruits() end
        if Config.FruitShop then OpenFruitShop() end
        if Config.IslandTeleport then TeleportIslands() end
        if Config.AllFeatures then EnableAllFeatures() end
    end
end)

-- CHỐNG AFK
local VirtualUser = game:GetService("VirtualUser")
game:GetService("Players").LocalPlayer.Idled:connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)